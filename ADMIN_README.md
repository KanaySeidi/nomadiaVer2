# 🔐 Админ Панель - Готовые Компоненты

## ✅ Что уже реализовано

### 1. **Страница логина** (`/admin/login`)

- Два поля: username и password
- Полная валидация полей
- Показать/скрыть пароль
- Состояние загрузки
- Обработка ошибок
- Адаптивный дизайн

### 2. **Auth Store** (Zustand + persist)

- Управление состоянием авторизации
- Сохранение токена в localStorage
- Автоматическое добавление токена в axios headers
- Функции: login, logout, checkAuth, clearError

### 3. **Protected Routes**

- Автоматическая защита админских роутов
- Редирект на логин если не авторизован
- Проверка токена перед доступом

### 4. **Admin Dashboard** (`/admin/dashboard`)

- Главная страница админки
- Карточки для управления: туры, сотрудники, галерея, видео
- Кнопка выхода
- Отображение имени пользователя

---

## 🚀 Как запустить

1. **Установите зависимости:**

```bash
npm install
```

2. **Запустите dev-сервер:**

```bash
npm run dev
```

3. **Откройте браузер:**

```
http://localhost:5173/admin/login
```

4. **Войдите с тестовыми данными:**

   - Username: `admin`
   - Password: `123456`

   ⚠️ **Внимание:** Пока что это временная проверка. После реализации API эндпоинта, данные будут проверяться на бэкенде.

---

## 📂 Структура файлов

```
src/
├── api/
│   └── authStore/
│       └── authStore.tsx          # Zustand store для авторизации
├── components/
│   └── pages/
│       └── admin/
│           ├── AdminLogin.tsx           # Страница логина
│           ├── AdminLogin.async.tsx     # Lazy loading
│           ├── AdminDashboard.tsx       # Главная админки
│           ├── AdminDashboard.async.tsx
│           └── ProtectedRoute.tsx       # Защита роутов
└── routes/
    └── routes.tsx                 # Конфигурация роутов
```

---

## 🔗 API Endpoints

**Документация:** См. файл `API_ENDPOINTS.md`

Основной эндпоинт для логина:

```
POST http://192.168.0.107:8820/api/auth/login
```

Request:

```json
{
  "username": "admin",
  "password": "123456"
}
```

Response:

```json
{
  "token": "jwt_token_here",
  "user": {
    "id": 1,
    "username": "admin",
    "role": "admin"
  }
}
```

---

## 🎯 Роуты

### Публичные

- `/` - Главная страница
- `/about` - О компании
- `/kyrgyzstan` - Туры Кыргызстан
- `/central` - Центральная Азия
- `/tour/:id` - Детали тура
- **`/admin/login`** - Вход в админку ✨

### Защищенные (требуют авторизации)

- **`/admin/dashboard`** - Главная админки ✨
- `/admin/tours/*` - Управление турами (TODO)
- `/admin/staff/*` - Управление сотрудниками (TODO)
- `/admin/pictures` - Галерея (TODO)
- `/admin/video` - Видео (TODO)

---

## 🛠 Технологии

- **React 19** + **TypeScript**
- **Zustand 5.0.8** - state management
- **React Router DOM 7.9.4** - маршрутизация
- **Axios** - HTTP клиент
- **Tailwind CSS 4** - стилизация
- **Vite 7** - сборщик

---

## 🔐 Безопасность

### Frontend

- ✅ Валидация форм
- ✅ Защищенные роуты
- ✅ Токен в localStorage + persist
- ✅ Автоматический редирект на login
- ✅ Очистка токена при logout

### Backend (TODO для backend разработчика)

- [ ] JWT токены
- [ ] Bcrypt для паролей
- [ ] Rate limiting
- [ ] CORS настройки
- [ ] HTTPS в продакшене

---

## 📝 Следующие шаги

### 1. Backend (приоритет!)

- [ ] Реализовать `POST /api/auth/login`
- [ ] Настроить JWT
- [ ] Создать middleware для проверки токена
- [ ] Добавить в .env секретный ключ для JWT

### 2. Frontend CRUD

- [ ] Страница управления турами
- [ ] Форма добавления/редактирования тура
- [ ] Страница управления сотрудниками
- [ ] Загрузка изображений
- [ ] Управление видео

### 3. UX улучшения

- [ ] Toast уведомления (успех/ошибка)
- [ ] Skeleton loaders
- [ ] Подтверждения удаления
- [ ] Пагинация списков

---

## 🐛 Известные особенности

1. **Временная проверка логина** в `AdminLogin.tsx`:

   ```typescript
   // Временная заглушка - удалить после реализации API
   if (formData.username === "admin" && formData.password === "123456") {
     // ...
   }
   ```

   Это нужно удалить, когда backend будет готов.

2. **Persist middleware**: Токен сохраняется в localStorage с ключом `auth-storage`.

3. **Автоматическое восстановление сессии**: При перезагрузке страницы токен восстанавливается из localStorage.

---

## 📞 Контакты

Если есть вопросы по интеграции или нужны изменения в структуре, дайте знать!

---

**Создано:** 01.11.2025
**Версия:** 1.0.0
